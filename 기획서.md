# 거시경제지표 대시보드 (Macro Dashboard) 기획서

> 작성일: 2026-09-08 · 버전 v1.0 · 상태: 개발 착수

---

## 1. 개요

### 1.1 배경
채권금리·CPI·기준금리 등 핵심 거시지표는 발표 시점마다 시장에 즉시 반영되지만,
실무자는 (1) FRED, (2) 미 재무부, (3) 한국은행 ECOS, (4) 연준 보도자료 등
서로 다른 소스를 매번 수동으로 확인해야 한다. 발표 일정과 주요 인사 발언까지
합치면 하루에 10곳 이상을 돌아다니게 된다.

### 1.2 목적
**"한 화면에서 지표 수치 · 과거 추이 · 발표 일정 · 정책당국 발언을 동시에 본다."**

### 1.3 핵심 요구사항 (사용자 정의)

| # | 요구사항 | 대응 기능 |
|---|---------|----------|
| 1 | 채권금리·CPI·기준금리 등 지표 수치 자동 업데이트 | 스케줄러 기반 수집기(Collector) |
| 2 | 대시보드 형태 시각화 | 단일 페이지 대시보드 (KPI 카드 + 차트) |
| 3 | 과거 data 연계 시각화 | 전 시계열 DB 적재 + 기간 토글(1Y/3Y/5Y/10Y/MAX) |
| 4 | 주요 경제지표 발표일정 캘린더 자동 업데이트 | 이벤트 캘린더 + FOMC/발표일정 수집 + iCal 내보내기 |
| 5 | 연준·미 재무장관 등 주요 인사 발언 업데이트 | RSS/보도자료 수집 + 매파-비둘기파 톤 태깅 |

### 1.4 비목표 (Out of Scope, v1)
- 투자 자문·매매 신호 생성 (본 서비스는 데이터 조회 도구이며 투자 권유가 아님)
- 실시간 틱 데이터 / 초 단위 스트리밍
- 사용자 계정·권한 관리 (v1은 로컬 단일 사용자 기준)

---

## 2. 사용자 시나리오

**A. 아침 브리핑 (08:30)**
대시보드 접속 → 상단 KPI에서 미 10년물 4.12% (전일 −3bp), 한국 기준금리 2.50% 확인
→ 캘린더에서 "오늘 21:30 미국 CPI 발표" 확인 → 발언 피드에서 어제 파월 연설 확인.

**B. 지표 발표 직후 (21:35)**
CPI 발표 → 스케줄러가 발표 직후 자동 재수집 → 카드에 신규 값 표시
→ 차트에서 최근 24개월 헤드라인/코어 CPI 추이 비교.

**C. 과거 국면 비교**
기간 토글 MAX → 2022년 인플레이션 급등기와 현재 구간을 같은 축에서 비교,
금리 인상 사이클(기준금리)과 10Y−2Y 스프레드 역전 구간을 함께 확인.

---

## 3. 시스템 아키텍처

```
┌──────────────────────────────────────────────────────────────┐
│                  Browser (SPA, Vanilla JS + ECharts)         │
│      KPI 카드 · 시계열 차트 · 수익률곡선 · 캘린더 · 발언 피드   │
└───────────────────────────┬──────────────────────────────────┘
                            │ REST (JSON)
┌───────────────────────────▼──────────────────────────────────┐
│                     FastAPI (app/main.py)                    │
│   /api/summary  /api/series  /api/calendar  /api/speeches    │
├──────────────────────────────────────────────────────────────┤
│   Services   : indicators / calendar / speeches              │
│   Scheduler  : 백그라운드 잡 (수집 주기 관리)                  │
│   Collectors : treasury · fred · ecos · fomc · rss           │
├──────────────────────────────────────────────────────────────┤
│                    SQLite (data/macro.db)                    │
│   indicators · observations · events · speeches · runs       │
└──────────────────────────────────────────────────────────────┘
                            │
    ┌───────────┬───────────┴────────┬──────────────┬──────────┐
    ▼           ▼                    ▼              ▼          ▼
 FRED API   US Treasury         BOK ECOS API    Fed RSS/HTML  Stooq
 (CPI, PCE,  (일별 국채          (한국 기준금리,  (연설·보도자료, (지수·환율)
  기준금리,   수익률곡선)          국고채, CPI)     FOMC 일정)
  고용)
```

### 3.1 기술 스택

| 레이어 | 선택 | 사유 |
|--------|------|------|
| 런타임 | Python 3.12 | 로컬에 설치됨 (Node.js 미설치 확인) |
| 백엔드 | FastAPI + Uvicorn | 이미 설치됨, 비동기 API + 자동 문서화 |
| DB | SQLite | 무설치·단일 파일, 시계열 수십만 행에 충분 |
| 스케줄 | 인프로세스 백그라운드 스레드 | 별도 데몬·크론 불필요 |
| 파싱 | requests + BeautifulSoup/lxml | 이미 설치됨 |
| 프론트 | Vanilla JS + ECharts(CDN) | 빌드 도구 불필요, 시계열 렌더 성능 우수 |

### 3.2 디렉터리 구조

```
거시 경세지표 대시보드/
├── 기획서.md                  # 본 문서
├── README.md                  # 실행 방법
├── requirements.txt
├── .env.example               # API 키 템플릿
├── run.py                     # 실행 엔트리포인트
├── app/
│   ├── config.py              # 설정·API 키 로딩
│   ├── db.py                  # 스키마·쿼리
│   ├── registry.py            # 지표 카탈로그 정의
│   ├── scheduler.py           # 수집 스케줄
│   ├── main.py                # FastAPI 라우트
│   ├── collectors/
│   │   ├── base.py            # 수집기 공통 인터페이스
│   │   ├── treasury.py        # 미 국채 수익률곡선 (키 불필요)
│   │   ├── fred.py            # 미국 거시지표 (키 필요)
│   │   ├── ecos.py            # 한국은행 (키 필요)
│   │   ├── calendar_src.py    # FOMC·발표 일정
│   │   └── speeches.py        # 연준/재무부 발언 RSS
│   └── services/
│       ├── indicators.py
│       ├── calendar.py
│       └── speeches.py
├── static/                    # 대시보드 UI
│   ├── index.html
│   ├── css/style.css
│   └── js/{app,charts,calendar,speeches}.js
└── data/macro.db
```

---

## 4. 데이터 소스 (접속 검증 완료)

| 소스 | 용도 | API 키 | 접속 테스트 | 갱신주기 |
|------|------|--------|------------|---------|
| **US Treasury** Daily Yield Curve CSV | 미 국채 1M~30Y 금리 | 불필요 | 200 OK | 영업일 1회 |
| **Federal Reserve** speeches.xml / press_all.xml | 연준 인사 연설·보도자료 | 불필요 | 200 OK | 1시간 |
| **Federal Reserve** fomccalendars.htm | FOMC 일정 | 불필요 | 200 OK | 1일 |
| **한국은행** 통화정책방향 결정회의 | 금통위 일정 | 불필요 | 200 OK | 1일 |
| **FRED API** | CPI, Core CPI, PCE, 기준금리, 실업률, GDP | 필요(무료) | 도달 확인(키 필요) | 1일 |
| **BOK ECOS API** | 한국 기준금리, 국고채, 소비자물가 | 필요(무료) | 도달 확인(키 필요) | 1일 |
| **Stooq** CSV | 주가지수·환율 등 보조지표 | 불필요 | 200 OK | 1일 |
| BLS 발표일정 페이지 | CPI/고용 발표일 | — | **403 차단** | → 시드 일정으로 대체 |

> **키 없이도 동작한다.** 키 미설정 시 Treasury·Fed·Stooq 기반으로 대시보드가 채워지고,
> 키가 필요한 카드는 "키 필요" 상태로 명시된다 (graceful degradation).
> 키를 넣으면 CPI·기준금리·한국 지표가 자동으로 활성화된다.

### 4.1 지표 카탈로그 (v1)
- **미국 금리** — 국채 3M·2Y·5Y·10Y·30Y, 10Y−2Y 스프레드, 연방기금금리(EFFR)
- **미국 물가·고용** — CPI YoY, Core CPI YoY, Core PCE YoY, 실업률, 비농업고용
- **한국** — 기준금리, 국고채 3Y·10Y, 소비자물가 YoY
- **시장** — S&P500, 달러인덱스, WTI, 금, 원/달러

---

## 5. 데이터 모델

```sql
indicators(code PK, name_ko, name_en, country, category, unit,
           source, source_id, frequency, transform, decimals, display_order)

observations(indicator_code, obs_date, value,
             PRIMARY KEY(indicator_code, obs_date))

events(id PK, event_date, event_time, country, title, importance,
       indicator_code, actual, forecast, previous, source,
       uid UNIQUE)

speeches(id PK, published_at, speaker, role, org, title, url,
         summary, tone, uid UNIQUE)

collection_runs(id PK, collector, started_at, finished_at,
                status, rows, message)
```

- `observations`는 (지표, 날짜) 복합 PK → UPSERT로 **멱등 수집** 보장
- `events.uid`, `speeches.uid`는 소스 고유키 → 중복 삽입 방지
- 과거 데이터는 삭제하지 않고 누적 (요구사항 3의 근간)

---

## 6. 기능 명세

### 6.1 자동 업데이트 (요구사항 1)

| 잡 | 주기 | 내용 |
|----|------|------|
| `treasury` | 1시간 | 미 국채 수익률곡선 |
| `fred` | 6시간 | CPI/PCE/고용/기준금리 |
| `ecos` | 6시간 | 한국 지표 |
| `speeches` | 1시간 | 연준·재무부 발언 |
| `calendar` | 12시간 | FOMC·주요 발표 일정 |

- 실패 시 수집기별로 예외를 격리하고 `collection_runs`에 결과 기록
- 대시보드 상단에 "마지막 갱신 시각 / 수집기 상태 배지" 노출
- `POST /api/refresh` 로 수동 즉시 수집 가능

### 6.2 대시보드 시각화 (요구사항 2)
- **KPI 카드**: 지표명 · 최신값 · 전기 대비(bp/%p) · 방향 색상 · 기준일
- **메인 차트**: 선택 지표 시계열, 다중 지표 오버레이(좌/우 축 분리)
- **수익률곡선 탭**: 최신 vs 1개월 전 vs 1년 전 곡선 비교
- 다크 테마 기본, 반응형(모바일 1열)

### 6.3 과거 데이터 연계 (요구사항 3)
- 기간 토글: 1Y / 3Y / 5Y / 10Y / MAX
- 최초 실행 시 **백필(backfill)**: 가용한 전 기간 이력을 일괄 적재
- 지표 간 비교 뷰 (예: 미 10Y 금리 vs Core CPI를 이중 축으로 동시 표시)

### 6.4 발표 일정 캘린더 (요구사항 4)
- 월간 캘린더 + "다가오는 7일" 리스트
- 중요도(높음/보통/낮음) 및 국가(US/KR) 필터
- FOMC 일정은 연준, 금통위 일정은 한국은행 페이지에서 자동 파싱, 미국 지표 발표일은 FRED 릴리스 캘린더
- `GET /api/calendar.ics` 로 개인 캘린더 구독 지원

### 6.5 주요 인사 발언 (요구사항 5)
- 대상: 연준 의장·부의장·이사·지역 연은 총재, 미 재무장관, 한국은행 총재
- 수집: 연준 speeches RSS·press_all RSS, 재무부 보도자료
- 키워드 기반 **매파(hawkish) / 비둘기파(dovish) / 중립** 자동 태깅
  - 매파: tighten, restrictive, inflation risk, higher for longer, vigilant …
  - 비둘기: cut, accommodative, downside risk, softening, moderating …
- 카드 피드: 날짜 · 인물 · 직책 · 제목 · 톤 배지 · 원문 링크

> 톤 태깅은 키워드 휴리스틱이며 해석 보조용이다. 판단은 원문 확인을 원칙으로 한다.

---

## 7. API 명세

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/summary` | KPI 카드용 전체 지표 최신값 + 변화량 |
| GET | `/api/indicators` | 지표 카탈로그 |
| GET | `/api/series?codes=US10Y,US2Y&range=5Y` | 시계열 데이터 |
| GET | `/api/curve` | 수익률곡선 비교(최신/1M/1Y) |
| GET | `/api/calendar?from=&to=&country=` | 발표 일정 |
| GET | `/api/calendar.ics` | iCal 구독 |
| GET | `/api/speeches?limit=30&tone=` | 발언 피드 |
| GET | `/api/status` | 수집기별 최근 실행 상태 |
| POST | `/api/refresh?collector=` | 수동 즉시 수집 |

---

## 8. 개발 로드맵

| 단계 | 범위 | 산출물 |
|------|------|--------|
| **M1 – 기반** | 프로젝트 구조, DB 스키마, 설정, 지표 카탈로그 | `config.py`, `db.py`, `registry.py` |
| **M2 – 수집** | Treasury/FRED/ECOS/RSS/캘린더 수집기 + 백필 | `app/collectors/*` |
| **M3 – API** | FastAPI 라우트, 서비스 계층 | `main.py`, `app/services/*` |
| **M4 – UI** | 대시보드 화면(KPI·차트·캘린더·발언) | `static/*` |
| **M5 – 자동화** | 스케줄러, 상태 모니터링 | `scheduler.py` |
| **M6 – 마감** | README, .env 가이드, 오류 처리 점검 | 문서 |

---

## 9. 리스크 및 대응

| 리스크 | 대응 |
|--------|------|
| BLS 등 일부 사이트 봇 차단(403) | 규칙 기반 일정 생성 + 시드 테이블로 대체 |
| API 키 미보유 상태 | 키 불필요 소스만으로 동작하는 축소 모드 |
| 소스 HTML 구조 변경 | 수집기별 예외 격리 — 한 곳이 깨져도 나머지는 정상 동작 |
| 소스 Rate limit | 수집 주기 보수적 설정 + 로컬 DB 캐시 우선 조회 |
| 수치 오차 | 원 출처 링크를 항상 함께 노출 |

---

## 10. 면책

본 서비스는 공개 데이터를 취합·표시하는 정보 도구이며 투자 자문이 아니다.
표시 수치는 원 출처와 상이할 수 있으므로, 의사결정 전 반드시 원문을 확인해야 한다.
